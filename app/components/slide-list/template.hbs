<div class="slide-list-container expanded">
  <div class="slide-list-menu" onclick={{action "toggleMenu"}}>
    {{fa-icon icon="bars"}}
  </div>
  {{#each slides as |slide index|}}
    <div class="p2 slide-list-li">
      <hr>
      <div class="slide-content">
        {{slide.content}}
      </div>
      <div class="slide-id">
        {{slide.id}}
      </div>
      <hr>
    </div>
  {{/each}}

  {{add-slide}}
</div>
